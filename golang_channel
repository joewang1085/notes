// 无缓存在第一次写入的时候也会阻塞（不能再同一个线程内部使用读写同一个无缓存的管道,新手坑！！），直到别的进程读取了管道 ！！！
 有缓存在第一次写入时不会阻塞，写满时写入才会阻塞


func aaa(){
	c := make(chan int ,0) // 无缓存管道
	c <- 0 //阻塞 直到有其他线程读取该管道
	<-c
}

func bbb(){
	c := make(chan int ,1) // 无缓存管道
	c <- 0 //不阻塞
	<-c //能输出
}

func ccc(){
	c := make(chan int ,1) // 无缓存管道
	c <- 0 //不阻塞
	c <- 1 //阻塞 ,管道已经塞满
	<-c //
}
